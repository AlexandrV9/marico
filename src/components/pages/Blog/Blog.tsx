import React from 'react';

import styles from './Blog.module.scss';

interface BlogProps {};

export const Blog = () => {
  return (
    <div className={styles.Blog}>
      Blog Component
    </div>
  );
};