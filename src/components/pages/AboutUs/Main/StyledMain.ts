import styled from "styled-components";

export default styled.main`
  max-width: 115.4rem;
  margin: 0 auto;

  .logo {
    max-width: 34rem;
    margin: 12.2rem auto 4.8rem;

    a {
      font-weight: 800;
      font-size: 62px;
      line-height: 93px;
    }
  }

  .getStarted {
    margin-bottom: 17.8rem;
  }
`;
